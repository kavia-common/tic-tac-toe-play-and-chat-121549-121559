# Environment variables for MongoDB consumers (backend and DB viewer)
# These mirror the values produced by startup.sh and db_visualizer/mongodb.env by default.

# Full MongoDB connection string with admin authentication source
MONGODB_URL="mongodb://appuser:dbuser123@localhost:5000/?authSource=admin"

# Target database name (matches DB_NAME default in startup.sh)
MONGODB_DB="myapp"

# Notes:
# - The Node.js database viewer (db_visualizer) reads from db_visualizer/mongodb.env by default.
# - Copy these values into your backend .env if needed for local development.
# - For preview environments, the orchestrator will set these variables appropriately.
